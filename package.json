{
  "name": "root",
  "private": true,
  "devDependencies": {
    "aws-sdk": "^2.503.0",
    "dotenv": "^8.0.0",
    "eslint": "^6.1.0",
    "lerna": "^3.15.0"
  },
  "scripts": {
    "bootstrap:ci": "lerna bootstrap --since --include-merged-tags",
    "build": "npm run bootstrap:ci && npm run saveConfig && lerna version patch",
    "saveConfig": "lerna exec --since --include-merged-tags -- node '../../publish.js'"
  }
}
