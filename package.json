{
  "name": "root",
  "private": true,
  "devDependencies": {
    "aws-sdk": "^2.503.0",
    "dotenv": "^8.0.0",
    "eslint": "^6.1.0",
    "eslint-config-standard": "^13.0.1",
    "eslint-plugin-import": "^2.18.2",
    "eslint-plugin-node": "^9.1.0",
    "eslint-plugin-promise": "^4.2.1",
    "eslint-plugin-standard": "^4.0.0",
    "lerna": "^3.15.0"
  },
  "scripts": {
    "bootstrap:ci": "lerna bootstrap --since --include-merged-tags",
    "build": "npm run bootstrap:ci && npm run pack && npm run saveConfig && lerna version patch --yes",
    "lint:ci": "lerna run lint --since --include-merged-tags",
    "pack": "lerna run build --since --include-merged-tags",
    "saveConfig": "lerna exec --since --include-merged-tags -- node '../../publish.js'"
  }
}
